version: "3.8"

services:
  apache-php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apache-php74
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./html:/var/www/html
    restart: unless-stopped
